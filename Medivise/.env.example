# Loaded by flutter_dotenv at startup (see lib/main.dart).
# pubspec.yaml declares .env as a bundled asset, so this file MUST exist
# for the app to build and run — copy it to `.env` before `flutter run`.
OPENAI_API_KEY=sk-your-key-here
