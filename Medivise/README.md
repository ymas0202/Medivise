# Medivise — Flutter client

The Flutter application for Medivise. See the [project README](../README.md) for what the
app does, the architecture, and setup instructions.

Quick start (from this directory):

```bash
cp .env.example .env
flutter pub get
flutter run
```

The app expects the Express backend in [`../backend`](../backend) to be running.
